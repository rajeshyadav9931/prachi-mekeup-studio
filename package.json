{
  "name": "prachi-makeup-studio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "build": "node build.mjs",
    "check": "node --check app.js && node --check server.mjs && node --check build.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
